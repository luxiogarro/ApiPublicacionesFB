name: 🚀 Deploy to cPanel (apipubli.luxio.dev)

on:
  push:
    branches:
      - main
      
jobs:
  web-deploy:
    name: 🎉 Subiendo archivos al servidor
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Obteniendo el código más reciente
      uses: actions/checkout@v4
    
    - name: 📂 Desplegando archivos vía FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.3
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        server-dir: /apipubli.luxio.dev/ # Directorio raíz destino en el FTP
        exclude: |
          **/.git*
          **/.git*/**
          **/config/config.php
          **/.github*/**
          GUIA_DESPLIEGUE_CPANEL.md
